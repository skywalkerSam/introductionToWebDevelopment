import { firstNumber } from "./module.mjs";

const a = firstNumber;
const b = 1024;

console.log(a);
console.log(b);
console.log(a, '+', b, '=', a + b);


// .mjs is the "required" file extention for importing modules in NodeJS.